services:
  backend:
    build:
      context: ./backend
    container_name: auto-manual-ai-backend
    ports:
      - "8000:8000"
    volumes:
      - ./backend/app:/app/app
    environment:
      - PYTHONUNBUFFERED=1
